apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "remindful.fullname" . }}-test-connection"
  labels:
    {{- include "remindful.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "remindful.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
